# PC System Auto Scripts 修改建议与 Agent 交接说明

审查日期：2026-09-17。对象：当前工作区 Python 源码；未验证 dist 中 exe 是否与源码一致。

## 可直接交给执行 Agent 的要求

请按下列优先级修复这个 Windows 电源监控与启动项管理工具。保留现有 CustomTkinter、主题、双语、托盘与精简模式，优先修复可靠性和操作安全问题。沿用现有函数和标准库，避免为了修复问题重写整个 UI 或引入大型框架。

每项修复交付：问题原因、修改位置、针对性的回归检查结果。系统写入测试使用 mock 或临时目录；真实电源方案、注册表和用户启动文件不可作为破坏性测试数据。未做实机验证的项目应明确标注。

下面的行号均对应审查时源码，后续以函数名定位为准。P1 为优先修复，P2 为随后改善。

## P1：优先修复

### 1. 监控开关无法持久保存

- 证据：`main.py:51` 的 `load_config()` 在读取配置后强制执行 `defaults["monitor_enabled"] = True`。隔离调用已验证：保存 false，读取结果仍为 true。
- 影响：用户关闭监控后，下次启动会再次自动切换电源方案。
- 修改：尊重已保存的布尔值；仅配置缺失时采用首次运行默认值。同时调整 `_force_quit()`：目前退出调用 `_stop_monitor()`，会把用户偏好写成 false。把“停止本次运行线程”和“保存用户关闭监控的选择”分开。
- 验收：开/关监控后分别退出重启，偏好均保持；正常退出不改变监控偏好。

### 2. 快速开关监控可能产生多个工作线程

- 证据：`power_manager.py:451` 的 `start()/stop()/_loop()` 共享 `_running`；stop 仅设 false，旧线程仍处于 `time.sleep()`；再次 start 将同一标志设 true 并新建线程。mock 验证 start→stop→start 创建两个线程，未等待旧线程结束。
- 影响：旧线程醒来后可继续工作，重复检查和切换；手动 `check_now()` 也可与后台检查同时执行。循环没有异常边界，异常退出后 UI 仍可能显示监控已开启。
- 修改：用 `threading.Event` 实现可中断等待，确保旧 worker 退出后才允许新 worker 工作；统一检查/切换的并发入口；异常记录并反馈监控状态。停止与退出不可长期阻塞 GUI。
- 验收：快速开关 20 次后至多一个监控 worker；手动检查与定时检查不会重复执行同一切换；注入异常后状态与真实 worker 一致；退出后无晚到的 UI 回调错误。

### 3. 启动文件夹条目可能删错文件

- 证据：`startup_manager.py:68` 去掉扩展名作为 name，`:138` 按 name 查找并删除第一个匹配文件；`main.py:1380` 也只用 source:name 作行标识。隔离检查中，demo.cmd 与 demo.lnk 同时存在时，传入 demo 删除的是列表中的第一个文件。
- 影响：点击某个条目，可能删除另一个同名文件；UI 行标识也会冲突。
- 修改：文件夹条目用规范化完整路径作为标识，删除函数接收具体条目或精确路径；验证路径位于对应启动文件夹。注册表条目继续使用注册表位置和完整值名作为标识。未知 source 应明确拒绝，不能默认为 HKCU 或用户启动文件夹。
- 验收：同目录 demo.cmd/demo.lnk 可独立展示和删除；删除任意一个不会影响另一个；非法来源和目录外路径被拒绝。

### 4. 添加、覆盖、删除启动项缺乏保护，启动命令不可靠

- 证据：`startup_manager.py:95` 直接 SetValueEx，会覆盖同名值；`main.py:1714` 点击删除直接执行，无确认和恢复；添加路径原样写入注册表。`:1622` 为源码启动拼命令，但 `get_app_exe_path()` 返回的 `sys.argv[0]` 可能是相对路径；exe 路径含空格时未统一加引号。
- 修改：新增时区分可执行文件路径与参数，正确引用路径；保留已有完整命令的兼容入口。源码自启动使用绝对脚本路径。同名覆盖必须明确提示。删除前显示名称、来源、命令/文件路径并确认；提供最小可用备份与恢复，文件删除优先可撤销方式。
- 验收：中文和空格路径、带参数命令、源码模式均能生成正确启动命令；取消覆盖/删除不产生写入；恢复能还原注册表原值及类型或原始文件。

### 5. “读取电源方案”会隐式修改系统

- 证据：`power_manager.py:371` 的 `get_all_plans()` 调用 `_restore_missing_builtin_schemes()`，进而执行 `powercfg /duplicatescheme`。构建下拉框、刷新界面、托盘和监控都会调用读取函数。
- 影响：用户仅打开或刷新软件，也可能新增电源方案。恢复失败信息被丢弃，进程内只尝试一次。
- 修改：让 get_all_plans 成为纯读取；把创建缺失性能方案作为明确的独立操作，说明将新增哪些方案并展示结果；操作失败允许重试。首次运行应让用户看见当前方案、目标和自动监控状态，再选择启用策略。
- 验收：打开、刷新、切换语言、构建托盘均不会调用 duplicatescheme/setactive；只有明确启用监控或执行切换/创建操作才改变系统状态；重复创建不产生冗余方案。

### 6. 电源方案身份和目标状态不一致

- 证据：`power_manager.py:97` 枚举时按 GUID 或名称判断，但 `:502` 监控只按名称判断；`main.py:1809` 的状态展示也只检查名称。隔离验证：内置高性能 GUID 被改名后，is_acceptable=True，而监控认为不合格。
- 另有问题：`main.py:891/:1781` 用显示名称映射 GUID，同名方案覆盖；托盘 `:2017` 按名称查找第一个方案。`_resolve_target()` 找不到指定 GUID 时静默回退，而 UI 可能显示 Auto，监控仍保留失效 GUID。
- 修改：统一方案判定逻辑，以 GUID 为身份；下拉框与托盘回调直接绑定 GUID，同名时加短 GUID 区分。目标被删除时明确提示，让用户重新选择或明确切换到自动策略；不要悄悄使用另一目标。自动选择优先级应固定。
- 验收：改名的内置高性能方案不反复纠正；同名方案可独立选择；失效目标不会造成持续重复切换或 UI 与后台策略不一致。

## P2：随后改善

### 7. 配置缺少校验，保存失败不可见

- 证据：`main.py:51–77` 直接合并任意 JSON、直接覆盖写文件并吞掉 OSError；配置存于 exe 目录。`PowerMonitor.__init__()` 没有经过 interval setter 校验，列宽也只做有限检查。
- 修改：校验顶层对象、布尔值、间隔范围、GUID、列宽数字与范围；损坏配置留备份后回退。使用同目录临时文件加原子替换；默认选择用户可写位置，并兼容迁移旧配置。保存失败反馈用户。
- 验收：顶层数组/null、字符串间隔、负间隔、异常列宽不会导致崩溃或忙循环；不可写目录给出错误；保存中断后旧配置仍可读取。

### 8. GUI 仍有同步系统查询，错误提示过于笼统

- 证据：`main.py:891` 初始化下拉框同步 get_all_plans；托盘构建也可能同步查询。`power_manager.py:137` 按编码循环重新运行命令，每次超时 10 秒，失败路径可累计约 40 秒；errors="replace" 并不能验证编码是否正确。注册表/方案读取多处把失败变成空结果。
- 修改：系统查询走后台，一次取得方案快照供 UI/托盘复用；子进程只执行一次，再解码字节。区分空列表、权限不足、超时和其他错误；增加标准库 logging 的滚动日志。现有后台线程直接调用 after 的路径，应检查关闭时序，必要时用 queue 由主线程统一取结果。
- 验收：模拟 powercfg 超时，窗口仍可交互；HKLM 无写权限时显示具体原因；读取失败不被显示成“没有启动项”；查询结束时窗口已关闭不会触发 Tk 错误。

### 9. 新增启动项后的列表与筛选、排序不一致

- 证据：`main.py:1679` 的 `_surgically_add_row()` 替换缓存后总是追加 UI 行，没有复用当前筛选/排序，也未删除同 key 的旧行。
- 修改：成功写入后走既有筛选、排序和渲染流程，或正确更新对应行。优先复用 `_sorted_startup_items()` 与现有刷新入口。
- 验收：覆盖同名项不会多一行；按名称排序时新增项位置正确；筛选 HKLM 时添加 HKCU 项不会混入当前列表；数量与显示一致。

### 10. 单实例机制存在启动竞态和端口误判

- 证据：`main.py:86–122` 先尝试连接固定端口，之后才构建 App 并异步 bind；任意服务占用该端口可能被认成已有实例。两次启动也可能同时通过检查，bind 失败的实例仍继续运行。accept 后的 recv 未设置连接超时。
- 修改：在创建 GUI 前原子获取当前用户/会话范围的实例锁，可用 Windows 命名 mutex；IPC 负责唤醒，增加协议确认、连接超时和资源释放。
- 验收：同时启动两次仅保留一个实例并唤醒窗口；无关服务占用原端口不会让应用误退出；连接后不发消息不会阻塞后续唤醒。

### 11. 构建流程可能删除运行配置，文档落后于实现

- 证据：`build.py` 删除整个 dist 和所有根目录 *.spec；当前 dist 内包含 config.json 和 app_icon.ico。README 仍称只管理 HKCU，源码已经读取 HKLM 和两个启动文件夹；缺少 start_to_tray 和精简模式说明。requirements 仅设下限，仓库未发现自动化测试。
- 修改：构建输出与运行数据分开，只清理构建拥有的产物，保留明确的打包配置；记录已验证依赖版本。更新功能范围和配置文档，建立覆盖上述故障的最小测试集。
- 验收：重新构建不删除用户配置；干净环境可按文档构建启动；明确源码版本与 exe 对应关系；上述 P1 问题都有回归检查。

## 后续产品建议（不属于已确认缺陷）

- 增加“仅观察”模式，让用户查看电源变化记录后再决定是否自动纠正。
- 允许按接通电源/使用电池设置不同目标，避免所有场景都维持高性能。
- 启动项显示启用状态，并提供可恢复的禁用/启用；当前 Run 值或启动文件存在，并不等于已核实 Windows 实际允许它启动。
- 首次手动启动优先展示窗口，并提供“登录后启动到托盘”的独立设置。当前 start_to_tray 默认 true，缺少明显配置入口。
- 做一轮真实窗口验收：中文/英文、100%/150%/200% 缩放、精简模式、长路径、键盘操作。现有设计 PNG 不应被当作当前程序的实机证据。

## 本次审查边界

已阅读核心源码并用隔离调用/mock 验证监控偏好覆盖、同名文件删除歧义、方案判定不一致，以及 start/stop/start 新建线程且未 join 的行为。其余结论来自静态调用链分析。未启动 GUI，未切换实际电源，未修改注册表或真实启动文件，未验证打包 exe。下一位 Agent 应在完成最小修复后补实机交互验收。
