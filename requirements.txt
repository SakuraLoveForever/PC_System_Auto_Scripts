# Runtime dependencies, pinned to the versions this project was verified with.
# Python 3.12+ (the app also imports ctypes.windll and winreg, so it is Windows-only).
customtkinter==5.2.2
pypinyin==0.55.0
pystray==0.19.5
Pillow==12.2.0

# Build-time only
pyinstaller==6.20.0
